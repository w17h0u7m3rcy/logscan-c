name: Release

on:
  push:
    tags:
      - "v*"

permissions:
  contents: write

jobs:
  build-windows:
    name: Build (Windows)
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4
      - uses: ilammy/msvc-dev-cmd@v1

      - name: Build (MSVC)
        shell: pwsh
        run: |
          cl /nologo /std:c11 /W4 /O2 .\src\logscan.c

      - name: Package (Windows)
        shell: pwsh
        run: |
          $tag = "${{ github.ref_name }}"
          Compress-Archive -Path .\logscan.exe -DestinationPath ".\logscan-$tag-windows-x86_64.zip" -Force

      - name: Upload artifact (Windows zip)
        uses: actions/upload-artifact@v4
        with:
          name: win-asset
          path: logscan-*-windows-x86_64.zip

  build-linux:
    name: Build (Linux)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Build (clang)
        run: |
          clang -std=c11 -Wall -Wextra -Wpedantic -Werror -O2 -o logscan ./src/logscan.c

      - name: Package (Linux)
        run: |
          tag="${GITHUB_REF_NAME}"
          tar -czf "logscan-${tag}-linux-x86_64.tar.gz" logscan

      - name: Upload artifact (Linux tar.gz)
        uses: actions/upload-artifact@v4
        with:
          name: linux-asset
          path: logscan-*-linux-x86_64.tar.gz

  publish:
    name: Publish Release
    runs-on: ubuntu-latest
    needs: [build-windows, build-linux]
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Download artifacts
        uses: actions/download-artifact@v4
        with:
          path: dist

      - name: Extract tag notes
        shell: bash
        run: |
          tag="${GITHUB_REF_NAME}"
          notes="$(git for-each-ref "refs/tags/${tag}" --format='%(contents)')"

          if [ -z "$notes" ]; then
            echo "No annotated tag message found (lightweight tag?)." > release_notes.md
            echo "" >> release_notes.md
            echo "If you want this auto-filled, create an annotated tag:" >> release_notes.md
            echo "  git tag -a ${tag} -m \"Release notes...\"" >> release_notes.md
          else
            printf "%s\n" "$notes" > release_notes.md
          fi

      - name: Create GitHub Release and upload assets
        uses: softprops/action-gh-release@v2
        with:
          body_path: release_notes.md
          files: |
            dist/**/logscan-*-windows-x86_64.zip
            dist/**/logscan-*-linux-x86_64.tar.gz